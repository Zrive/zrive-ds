-orders.parquet: An order history of customers. Each row is an order and the
-item_ids for the order are stored as a list in the item_ids column
-regulars.parquet: Users are allowed to specify items that they wish to buy
regularly. This data gives the items each user has asked to get regularly, along
with when they input that information.
abandoned_cart.parquet: If a user has added items to their basket but not
bought them, we capture that information. Items that were abandoned are stored
as a list in item_ids.
-inventory.parquet: Some information about each item_id
-users.parquet: Information about users.

1. Understanding the problem space
    1 - Do all the quick checks and find any potential issues with the data. Fix them as you see fit.
            1. orders >> Todo parece OK aunque no sé bien como hacer el match entre ordered_items y el variant_id de los otros datasets. Se me ocurre hacer otra columna con el espacio temportal 
                        entre created_at y order_date para obtener aún mas datos significativos como que productos generan mas dudas al consumidor, compras impulsivas, tipos de consumidores, etc...
            2. regulars >> Todo OK, genial para generar sistemas de recomendaciones.
            3. abandoned_cart >> El data type de "variant_ID" es object no int64 como en los datasets de regulars e inventory
            4. inventory >> No entiendo bien algunas columans como "compare_at_price"
            5. users >> Más del 93% de los datos en 5 categorias son Null, parece que se dejaron de pedir por algun motivo (quizas temas de privacidad) ya que tienen el mismo numero de entradas.

    2 - Understand the problem at hand through data interrogation and hypothesis testing.
            Después de analizar el dataset de feature_frame el problema esta más claro > Buscamos predecir si un producto se va a comprar o no o las probabilidades de que esto ocurra en función 
            de la información previa proporcionada durante el proceso de compra y otra serie de informació recopilada del posible comprador
            Hypothesis
                - Habrá una serie de variables que estén aportando ruido a nuestro modelo ya que la información se encointrará ya almacenada en otra variable más relevante
                - Puede ser que existan productos menos populares por su descatalogazicación u otros factores, debemos analizarlos y decidir si mantenerlos o no
                - Los datos de adults, children, baby, etc... pueden haberse dejado de pedir por un tema de privacidad y de ser así quizás hagan que nuestra muestra esté corrompida
                - Por el tipo de dataset no parece que puedan existir outliers aunque no se si también puede haber en este tipo de datos (preguntar en clase)
                - Hay muchas variables como outco

    3 - Gather all the hypothesis and insights you have tested and found, in a document or clean jupyte